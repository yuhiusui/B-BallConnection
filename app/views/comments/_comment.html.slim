.comment-form
  = form_with model:[court, comment], data: {remote: true}, id: 'comment-form' do |f|
    .row
      .form-group.col-4.ml-auto
        = f.text_area :comment, placeholder: "コメント(3~50文字)", class: "form-control"
        = f.hidden_field :score, id: "comment-score"
      .col-4
        = button_tag '投稿する',  type: 'button', id: "comment", class: "btn btn-success btn-lg"
