- if court.liked_by?(current_player)
  = link_to court.likes.count, court_likes_path(court), method: :delete, remote: true, class: "fa fa-heart"
- else
  = link_to court.likes.count, court_likes_path(court), method: :post, remote: true, class: "fa fa-heart-o"